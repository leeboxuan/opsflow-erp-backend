generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model Tenant {
  id                           String                            @id @default(cuid())
  name                         String
  slug                         String                            @unique
  timezone                     String?
  /// Lifecycle: SETUP (config), ACTIVE, SUSPENDED (ordinary users blocked), ARCHIVED
  status                       TenantStatus                      @default(ACTIVE)
  createdAt                    DateTime                          @default(now())
  updatedAt                    DateTime                          @updatedAt
  driver_wallet_entries        driver_wallet_entries[]
  driverWalletTransactions     DriverWalletTransaction[]
  drivers                      drivers[]
  driverLocationLatest         DriverLocationLatest[]
  eventLogs                    EventLog[]
  inventory_items              inventory_items[]
  inventory_batches            inventory_batches[]
  inventory_batch_items        inventory_batch_items[]
  inventory_units              inventory_units[]
  podPhotoDocuments            PodPhotoDocument[]
  pods                         Pod[]
  stops                        Stop[]
  memberships                  TenantMembership[]
  transportOrders              TransportOrder[]
  transport_order_items        transport_order_items[]
  trips                        Trip[]
  vehicles                     Vehicle[]
  fleetVehicles                FleetVehicle[]
  transport_order_item_units   transport_order_item_units[]
  customer_companies           customer_companies[]
  Invoice                      Invoice[]
  InvoiceLineItem              InvoiceLineItem[]
  transportInternalRefCounters transport_internal_ref_counters[]
  jobs                         Job[]
  jobTypeAssignments           JobTypeAssignment[]
  invoiceChargeReservations    InvoiceChargeReservation[]
  jobDocuments                 JobDocument[]
  jobInternalRefCounters       job_internal_ref_counters[]
  auditLogs                    AuditLog[]
  JobItem                      JobItem[]
  customerCompanyQuotations    CustomerCompanyQuotation[]
  customerCompanyDocuments     CustomerCompanyDocument[]
  customerQuotationRateLines   CustomerQuotationRateLine[]
  customerRateMasterLines      CustomerRateMasterLine[]
  customerRateTemplates        CustomerRateTemplate[]
  customerRateTemplateRows     CustomerRateTemplateRow[]
  customerQuotations           CustomerQuotation[]
  idempotencyRecords           IdempotencyRecord[]
  customerQuotationLines       CustomerQuotationLine[]
  quotationNoCounters          quotation_no_counters[]
  masterFiles                  MasterFile[]
  customerQuotationItems       CustomerQuotationItem[]
  tenantQuotationItems         TenantQuotationItem[]
  masterRateDatasets           MasterRateDataset[]
  masterRateDatasetRows        MasterRateDatasetRow[]
  driverPayoutItems            DriverPayoutItem[]
  dhcReferenceItems            DhcReferenceItem[]
  jobCharges                   JobCharge[]
  depotHandlingReferences      DepotHandlingReference[]
  driverTripRateMasters        DriverTripRateMaster[]
  tripDocuments                TripDocument[]
  tripPayoutLines              TripPayoutLine[]
  tripDocumentRequirements     TripDocumentRequirement[]
  tripExpenses                 TripExpense[]
  tripExpenseAttachments       TripExpenseAttachment[]
  tripExpenseEvents            TripExpenseEvent[]
  notifications                Notification[]
  pushDevices                  PushDevice[]
  chassis                      Chassis[]
  gpsDevices                   GpsDevice[]
  gpsPositions                 GpsPosition[]
  warehouseJobs                WarehouseJob[]
  warehouseJobLines            WarehouseJobLine[]
  warehouseJobUnits            WarehouseJobUnit[]
  warehouseJobEvents           WarehouseJobEvent[]
  warehouseJobInternalRefCounters warehouse_job_internal_ref_counters[]
  warehouseJobCustomerRefCounters warehouse_job_customer_ref_counters[]
  warehouseJobDocuments        WarehouseJobDocument[]
  warehouseJobCargoLines       WarehouseJobCargoLine[]
  warehouseJobContainers       WarehouseJobContainer[]
  tripJobItems                 TripJobItem[]
  moduleEntitlements           TenantModuleEntitlement[]
  platformAuditLogsAsTarget    PlatformAuditLog[]                @relation("PlatformAuditTargetTenant")

  @@map("tenants")
}

model User {
  id         String  @id @default(cuid())
  /// Supabase Auth user id (auth.users.id, UUID). Links JWT sub to this row; not the same as id (cuid).
  authUserId String? @unique
  email      String  @unique
  /// Optional login username (warehouse / driver mobile).
  /// Application persists canonical form via normalizeUsername (trim, lowercase, strip \s+).
  /// Global uniqueness is NOT Prisma @unique on the raw column. PostgreSQL enforces
  /// users_username_normalized_key: unique(lower(regexp_replace(username, '[[:space:]]+', '', 'g')))
  /// WHERE username IS NOT NULL AND the expression is non-empty. Multiple NULLs remain valid.
  username   String?
  name       String?
  displayName String?
  phone      String?
  avatarKey  String?
  avatarUrl  String?
  avatarUpdatedAt DateTime?

  createdAt   DateTime           @default(now())
  updatedAt   DateTime           @updatedAt
  /// Application-level role for this user (e.g. 'USER', 'SUPERADMIN').
  /// SUPERADMIN is NOT a tenant Role — platform authority is PlatformAdmin.
  role        UserRole           @default(USER)
  drivers     drivers[]
  profiles    profiles?
  memberships TenantMembership[]
  membershipRolesCreated TenantMembershipRole[] @relation("TenantMembershipRoleCreatedBy")
  platformAdmin PlatformAdmin?
  platformAdminsCreated PlatformAdmin[] @relation("PlatformAdminCreatedBy")
  platformAuditLogsAsActor PlatformAuditLog[] @relation("PlatformAuditActorUser")
  tripExpensesSubmitted TripExpense[] @relation("TripExpenseSubmittedBy")
  tripExpensesReviewed  TripExpense[] @relation("TripExpenseReviewedBy")
  tripExpensesReimbursed TripExpense[] @relation("TripExpenseReimbursedBy")
  tripExpenseAttachmentsUploaded TripExpenseAttachment[] @relation("TripExpenseAttachmentUploadedBy")
  tripExpenseEventsActor TripExpenseEvent[] @relation("TripExpenseEventActor")

  customerCompanyId String?
  customerContactId String?

  customerCompany  customer_companies? @relation("UserCustomerCompany", fields: [customerCompanyId], references: [id], onDelete: SetNull)
  customerContact  customer_contacts?  @relation("UserCustomerContact", fields: [customerContactId], references: [id], onDelete: SetNull)
  jobsAssigned     Job[]               @relation("JobAssignedDriver")
  vehiclesAssigned Vehicle[]           @relation("VehicleDriver")
  fleetVehiclesAssigned FleetVehicle[] @relation("FleetVehicleDriver")
  jobsCreated      Job[]               @relation("JobCreatedBy")
  quotationsUploaded CustomerCompanyQuotation[] @relation("QuotationUploadedBy")
  customerCompanyDocumentsUploaded CustomerCompanyDocument[] @relation("CustomerCompanyDocumentUploadedBy")
  customerCompanyDocumentsGenerated CustomerCompanyDocument[] @relation("CustomerCompanyDocumentGeneratedBy")
  jobChargesSelected JobCharge[]       @relation("JobChargeSelectedBy")
  masterFilesUploaded MasterFile[]      @relation("MasterFileUploadedBy")
  jobDocumentsUploaded JobDocument[]   @relation("JobDocumentUploadedBy")
  tripDocumentsUploaded TripDocument[] @relation("TripDocumentUploadedBy")
  warehouseJobsAssignedTo WarehouseJob[]      @relation("WarehouseJobAssignedTo")
  warehouseJobsCsInCharge WarehouseJob[]      @relation("WarehouseJobCsInCharge")
  warehouseJobsCreated    WarehouseJob[]      @relation("WarehouseJobCreatedBy")
  warehouseJobEventsActed WarehouseJobEvent[] @relation("WarehouseJobEventActor")
  warehouseJobDocumentsUploaded WarehouseJobDocument[] @relation("WarehouseJobDocumentUploadedBy")
  warehouseJobDocumentsReviewed WarehouseJobDocument[] @relation("WarehouseJobDocumentReviewedBy")
  tripJobItemsLinked            TripJobItem[]          @relation("TripJobItemLinkedBy")

  customerRateTemplatesCreated  CustomerRateTemplate[] @relation("CustomerRateTemplateCreatedBy")
  customerRateTemplatesUpdated  CustomerRateTemplate[] @relation("CustomerRateTemplateUpdatedBy")
  customerQuotationsCreated     CustomerQuotation[]    @relation("CustomerQuotationCreatedBy")
  customerQuotationsUpdated     CustomerQuotation[]    @relation("CustomerQuotationUpdatedBy")
  customerQuotationsIssued      CustomerQuotation[]    @relation("CustomerQuotationIssuedBy")
  customerQuotationsAccepted    CustomerQuotation[]    @relation("CustomerQuotationAcceptedBy")

  @@index([customerCompanyId])
  @@index([customerContactId])
  @@index([email])
  @@index([authUserId])
  @@map("users")
}

model customer_companies {
  id       String @id @default(cuid())
  tenantId String

  // Core identity
  name           String
  normalizedName String

  // Company contact
  email String?
  phone String?

  // Address
  addressLine1 String?
  addressLine2 String?
  postalCode   String?
  country      String? @default("SG")

  // Billing
  billingSameAsAddress Boolean @default(false)
  billingAddressLine1  String?
  billingAddressLine2  String?
  billingPostalCode    String?
  billingCountry       String? @default("SG")

  // PIC
  picName   String?
  picMobile String?
  picEmail  String?

  // Helpful extras
  uen      String?
  notes    String?
  isActive Boolean @default(true)
  /// Commercial lifecycle. Missing signed quotation may block ACTIVE, not create.
  commercialStatus CustomerCommercialStatus @default(PROSPECT)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant          Tenant              @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  contacts        customer_contacts[]
  batches         inventory_batches[] @relation("InventoryBatchCustomerCompany")
  users           User[]              @relation("UserCustomerCompany")
  transportOrders TransportOrder[]    @relation("TransportOrderCustomerCompany")
  jobs            Job[]               @relation("JobCustomerCompany")
  warehouseJobs   WarehouseJob[]      @relation("WarehouseJobCustomerCompany")
  quotations      CustomerCompanyQuotation[]
  documents       CustomerCompanyDocument[]
  rateMasterLines CustomerRateMasterLine[]
  rateTemplates   CustomerRateTemplate[]
  customerQuotations CustomerQuotation[]
  masterFiles     MasterFile[]

  @@unique([tenantId, normalizedName])
  @@index([tenantId])
  @@index([tenantId, isActive])
  @@index([tenantId, commercialStatus])
  @@map("customer_companies")
}

model customer_contacts {
  id              String   @id @default(cuid())
  companyId       String
  name            String
  email           String
  normalizedEmail String
  mobile          String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  company customer_companies  @relation(fields: [companyId], references: [id], onDelete: Cascade)
  batches inventory_batches[] @relation("InventoryBatchCustomerContact")
  users   User[]              @relation("UserCustomerContact")

  @@unique([companyId, normalizedEmail])
  @@index([companyId])
  @@map("customer_contacts")
}

model TenantMembership {
  id        String           @id @default(cuid())
  tenantId  String
  userId    String
  /// Transitional/legacy single-role column. Canonical authorization uses membershipRoles.
  role      Role
  status    MembershipStatus @default(Active)
  createdAt DateTime         @default(now())
  updatedAt DateTime         @updatedAt
  tenant    Tenant           @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  user      User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  membershipRoles TenantMembershipRole[]

  @@unique([tenantId, userId])
  @@index([tenantId, status])
  @@index([userId])
  @@map("tenant_memberships")
}

/// Canonical tenant authorization roles. A membership may hold many rows.
/// PLATFORM_ADMIN is not a tenant membership role.
model TenantMembershipRole {
  id                 String              @id @default(cuid())
  tenantMembershipId String
  role               CanonicalTenantRole
  createdAt          DateTime            @default(now())
  createdByUserId    String?
  membership         TenantMembership    @relation(fields: [tenantMembershipId], references: [id], onDelete: Cascade)
  createdBy          User?               @relation("TenantMembershipRoleCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)

  @@unique([tenantMembershipId, role])
  @@index([tenantMembershipId])
  @@index([role])
  @@map("tenant_membership_roles")
}

model TransportOrder {
  id                    String              @id @default(cuid())
  tenantId              String
  customerRef           String
  status                OrderStatus         @default(Open)
  pickupWindowStart     DateTime?
  pickupWindowEnd       DateTime?
  deliveryWindowStart   DateTime?
  deliveryWindowEnd     DateTime?
  notes                 String?
  customerContactNumber String?
  customerCompanyId     String?
  customerCompany       customer_companies? @relation("TransportOrderCustomerCompany", fields: [customerCompanyId], references: [id], onDelete: SetNull)

  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  priceCents     Int?
  currency       String?   @default("SGD")
  customerName   String?
  doDocumentUrl  String?
  doSignatureUrl String?
  doSignedAt     DateTime?
  doSignerName   String?
  doStatus       DoStatus  @default(PENDING)
  doVersion      Int       @default(1)
  // Client's reference (what they give you)
  orderRef       String

  // Internal OpsFlow reference (DB-YYYY-MM-DD-XXX)
  internalRef                String?
  driver_wallet_transactions DriverWalletTransaction[]
  stops                      Stop[]                    @relation("OrderStops")
  transport_order_items      transport_order_items[]
  inventory_units            inventory_units[]
  tenant                     Tenant                    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  invoiceId                  String?
  invoice                    Invoice?                  @relation(fields: [invoiceId], references: [id], onDelete: SetNull)

  @@unique([tenantId, orderRef])
  @@unique([tenantId, internalRef])
  @@index([tenantId, customerCompanyId])
  @@index([tenantId, status])
  @@index([tenantId, createdAt])
  @@index([tenantId, invoiceId])
  @@map("transport_orders")
}

model Trip {
  id                   String                    @id @default(cuid())
  tenantId             String
  status               TripStatus                @default(DRAFT)
  pendingState         TripPendingState          @default(NONE)
  plannedStartAt       DateTime?
  plannedEndAt         DateTime?
  createdAt            DateTime                  @default(now())
  updatedAt            DateTime                  @updatedAt
  createdByUserId      String?
  publishedAt          DateTime?
  publishedByUserId    String?
  assignedAt           DateTime?
  assignedByUserId     String?
  updatedByUserId      String?
  closedAt             DateTime?
  startedAt            DateTime?
  acceptedTrailerNo    String?
  acceptedVehicleNo    String?
  assignedDriverUserId String?
  driverId             String?
  routeVersion         Int                       @default(1)
  vehicleId            String?
  fleetVehicleId       String?
  /// Hard constraint: trip includes a PSA port stop and requires an authorised driver.
  /// Not inferred from IMPORT/EXPORT alone. Default false.
  requiresPsaPortAccess Boolean                  @default(false)
  /// Phase 4: what this trip is performing. Nullable until expand/backfill/contract migration is applied.
  tripType                JobType?
  /// When set, this trip belongs to an OpsFlow job (multi-leg). Transport-order trips keep jobId null.
  jobId                String?
  /// Sequence within the job (1-based). Null for legacy transport-only trips.
  jobSequence          Int?
  tripSequence         Int?
  /// Phase 5: driver-day dispatch run order (independent of job-local tripSequence/jobSequence).
  dispatchSequence     Int?
  /// Phase 5: optimistic concurrency for dispatch planning (independent of routeVersion).
  dispatchVersion      Int                       @default(1)
  displayTitle         String?
  jobTripTemplate      JobTripTemplate?
  title                String?
  startedByDriverUserId   String?
  completedByDriverUserId String?
  /// Cached summary only. Source of truth is TripPayoutLine.
  driverEarningCents      Int?
  earningRateMasterId     String?
  payoutItemId            String?
  earningLabelSnapshot    String?
  trailerNumber           String?
  trailerLastLocationCode String?
  trailerParkingAddress1  String?
  trailerParkingAddress2  String?
  trailerParkingPostal    String?
  trailerParkingPlaceId   String?
  /// Trip-specific ops/driver instructions (distinct from job-level notes).
  notes                   String?                   @db.Text
  /// Driver-owned operational remarks (distinct from Job.description / Job.notes).
  driverRemarks           String?                   @db.Text
  tripPICName             String?
  tripPICContact          String?
  containerNumber         String?
  carrier                 String?
  shipper                 String?
  vessel                  String?
  trailerParkingLat       Float?
  trailerParkingLng       Float?
  trailerParkedAt         DateTime?
  /// e.g. { "requireJobDoSigned": true, "requiredTripUploadTypes": ["OFFLOADING"] }
  completionRuleJson      Json?
  originLocationId        String?
  originLabel             String?
  originAddressLine1      String?
  originAddressLine2      String?
  originPostalCode        String?
  originCountry           String?
  originLat               Float?
  originLng               Float?
  originPlaceId           String?
  destinationLocationId   String?
  destinationLabel        String?
  destinationAddressLine1 String?
  destinationAddressLine2 String?
  destinationPostalCode   String?
  destinationCountry      String?
  destinationLat          Float?
  destinationLng          Float?
  destinationPlaceId      String?
  walletTransactions   DriverWalletTransaction[]
  stops                Stop[]
  inventory_units      inventory_units[]
  drivers              drivers?                  @relation(fields: [driverId], references: [id])
  tenant               Tenant                    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  vehicles             Vehicle?                  @relation(fields: [vehicleId], references: [id])
  fleetVehicle         FleetVehicle?             @relation(fields: [fleetVehicleId], references: [id])
  job                  Job?                      @relation(fields: [jobId], references: [id], onDelete: Cascade)
  earningRateMaster    DriverTripRateMaster?     @relation(fields: [earningRateMasterId], references: [id], onDelete: SetNull)
  payoutItem           DriverPayoutItem?         @relation(fields: [payoutItemId], references: [id], onDelete: SetNull)
  documents            TripDocument[]
  payoutLines          TripPayoutLine[]
  documentRequirements TripDocumentRequirement[]
  /// Canonical M:N cargo linkage. Trip.containerNumber is legacy/display cache only.
  tripJobItems         TripJobItem[]
  tripExpenses         TripExpense[]

  @@index([tenantId, status])
  @@index([tenantId, plannedStartAt])
  @@index([tenantId, assignedDriverUserId])
  @@index([tenantId, assignedDriverUserId, status, plannedStartAt])
  @@index([tenantId, assignedDriverUserId, dispatchSequence])
  @@index([tenantId, driverId])
  @@index([tenantId, vehicleId])
  @@index([tenantId, fleetVehicleId])
  @@index([tenantId, payoutItemId])
  @@index([tenantId, jobId])
  @@index([tenantId, jobId, jobSequence])
  @@index([tenantId, jobId, tripType])
  @@map("trips")
}

model MasterLogisticsLocation {
  id           String                @id @default(cuid())
  code         String
  name         String
  label        String?
  type         LogisticsLocationType
  addressLine1 String
  addressLine2 String?
  postalCode   String?
  country      String                @default("SG")
  lat          Float?
  lng          Float?
  placeId      String?
  isActive     Boolean               @default(true)
  sortOrder    Int                   @default(0)
  createdAt    DateTime              @default(now())
  updatedAt    DateTime              @updatedAt

  @@unique([code])
  @@unique([type, code])
  @@index([type, isActive])
  @@map("master_logistics_locations")
}

model MasterFile {
  id               String            @id @default(cuid())
  tenantId         String
  customerCompanyId String?
  type             MasterFileType
  fileName         String
  fileUrl          String
  uploadedByUserId String?
  uploadedAt       DateTime          @default(now())
  effectiveDate    DateTime?
  status           MasterFileStatus  @default(PARSED)
  parseSummaryJson Json?
  isActive         Boolean           @default(true)
  createdAt        DateTime          @default(now())
  updatedAt        DateTime          @updatedAt

  tenant                 Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customerCompany        customer_companies?     @relation(fields: [customerCompanyId], references: [id], onDelete: SetNull)
  uploadedBy             User?                   @relation("MasterFileUploadedBy", fields: [uploadedByUserId], references: [id], onDelete: SetNull)
  customerQuotationItems CustomerQuotationItem[]
  driverPayoutItems      DriverPayoutItem[]
  dhcReferenceItems      DhcReferenceItem[]

  @@index([tenantId, customerCompanyId, type, isActive])
  @@index([tenantId, type, uploadedAt])
  @@map("master_files")
}

model CustomerQuotationItem {
  id           String   @id @default(cuid())
  tenantId     String
  masterFileId String
  section      String?
  code         String
  label        String
  description  String?
  category     String?
  containerSize String?
  tripMode     String?
  areaScope    String?
  unit         String?
  rateCents    Int?
  requiresManualAmount Boolean @default(false)
  rawRateText  String?
  notes        String?
  sortOrder    Int      @default(0)
  active       Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  tenant     Tenant     @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  masterFile MasterFile @relation(fields: [masterFileId], references: [id], onDelete: Cascade)

  @@index([tenantId, masterFileId])
  @@index([tenantId, active, code])
  @@map("customer_quotation_items")
}

model TenantQuotationItem {
  id                   String   @id @default(cuid())
  tenantId             String
  section              String?
  code                 String
  label                String
  description          String?
  category             String?
  containerSize        String?
  tripMode             String?
  areaScope            String?
  unit                 String?
  rateCents            Int?
  requiresManualAmount Boolean  @default(false)
  rawRateText          String?
  notes                String?
  sortOrder            Int      @default(0)
  active               Boolean  @default(true)
  sourceType           String   @default("EXCEL_IMPORT")
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId, active, sortOrder])
  @@index([tenantId, code])
  @@map("tenant_quotation_items")
}

model MasterRateDataset {
  id               String                @id @default(cuid())
  tenantId         String
  type             MasterRateDatasetType
  versionNo        Int
  status           MasterRateDatasetStatus @default(DRAFT)
  /// Exactly one current base template per (tenantId, type). Enforced by partial unique index.
  isCurrent        Boolean               @default(false)
  createdAt        DateTime              @default(now())
  createdByUserId  String?
  updatedAt        DateTime              @updatedAt
  updatedByUserId  String?
  importedAt       DateTime?
  importedByUserId String?
  sourceFileName   String?
  activatedAt      DateTime?
  activatedByUserId String?

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  rows   MasterRateDatasetRow[]

  @@index([tenantId, type, status])
  @@index([tenantId, type, isCurrent])
  @@unique([tenantId, type, versionNo])
  @@map("master_rate_datasets")
}

model MasterRateDatasetRow {
  id                     String   @id @default(cuid())
  datasetId              String
  tenantId               String
  code                   String
  label                  String
  section                String?
  description            String?
  category               String?
  unit                   String?
  containerSize          String?
  tripMode               String?
  areaScope              String?
  currency               String   @default("SGD")
  rateCents              Int?
  rawRateText            String?
  requiresManualAmount   Boolean  @default(false)
  hasMultipleRates       Boolean  @default(false)
  rateOptionsJson        Json?
  defaultRateOptionIndex Int?
  notes                  String?
  sortOrder              Int?
  isActive               Boolean  @default(true)
  metadataJson           Json?
  createdAt              DateTime @default(now())
  createdByUserId        String?
  updatedAt              DateTime @updatedAt
  updatedByUserId        String?

  dataset MasterRateDataset @relation(fields: [datasetId], references: [id], onDelete: Cascade)
  tenant  Tenant            @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  tripPayoutLines TripPayoutLine[]

  @@index([tenantId, datasetId])
  @@index([tenantId, code])
  @@index([tenantId, isActive, sortOrder])
  @@map("master_rate_dataset_rows")
}

model DriverPayoutItem {
  id                      String   @id @default(cuid())
  tenantId                String
  masterFileId            String
  section                 String?
  code                    String
  label                   String
  description             String?
  category                String?
  containerSize           String?
  tripMode                String?
  areaScope               String?
  unit                    String?
  rateCents               Int?
  requiresManualAmount    Boolean @default(false)
  rawRateText             String?
  notes                   String?
  isSelectableForTripEarning Boolean @default(true)
  sortOrder               Int      @default(0)
  active                  Boolean  @default(true)
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt

  tenant     Tenant     @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  masterFile MasterFile @relation(fields: [masterFileId], references: [id], onDelete: Cascade)
  trips      Trip[]
  tripPayoutLines TripPayoutLine[]

  @@index([tenantId, masterFileId])
  @@index([tenantId, active, code])
  @@map("driver_payout_items")
}

model DhcReferenceItem {
  id           String   @id @default(cuid())
  tenantId     String
  masterFileId String
  section      String?
  code         String
  label        String
  description  String?
  category     String?
  unit         String?
  rateCents    Int?
  yardDepot    String?
  oldRateCents Int?
  newRateCents Int?
  software     String?
  operatorCode String?
  operatorName String?
  effectiveDate DateTime?
  notes        String?
  sortOrder    Int      @default(0)
  active       Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  tenant     Tenant     @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  masterFile MasterFile @relation(fields: [masterFileId], references: [id], onDelete: Cascade)

  @@index([tenantId, masterFileId])
  @@index([tenantId, active, code])
  @@map("dhc_reference_items")
}

model Vehicle {
  id                 String        @id @default(cuid())
  tenantId           String
  plateNo            String
  type               VehicleType
  status             VehicleStatus @default(ACTIVE)
  vehicleDescription String?
  driverId           String?
  roadTaxExpiryDate  DateTime?
  lastServicingDate  DateTime?
  coeExpiryDate      DateTime?
  createdAt          DateTime      @default(now())
  updatedAt          DateTime      @updatedAt
  trips              Trip[]
  tenant             Tenant        @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  driversDefault     drivers[]
  driver             User?         @relation("VehicleDriver", fields: [driverId], references: [id], onDelete: SetNull)
  gpsDevices         GpsDevice[]
  gpsPositions       GpsPosition[]

  @@unique([tenantId, plateNo])
  @@index([tenantId])
  @@index([tenantId, status])
  @@index([tenantId, type])
  @@index([tenantId, driverId])
  @@map("vehicles")
}

model FleetVehicle {
  id                 String        @id @default(cuid())
  tenantId           String
  plateNo            String
  type               VehicleType
  status             VehicleStatus @default(ACTIVE)
  vehicleDescription String?
  driverId           String?
  roadTaxExpiryDate  DateTime?
  lastServicingDate  DateTime?
  coeExpiryDate      DateTime?
  createdAt          DateTime      @default(now())
  updatedAt          DateTime      @updatedAt

  tenant         Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  driver         User?    @relation("FleetVehicleDriver", fields: [driverId], references: [id], onDelete: SetNull)
  driversDefault drivers[]
  jobsAssigned   Job[]
  trips          Trip[]

  @@unique([tenantId, plateNo])
  @@index([tenantId])
  @@index([tenantId, status])
  @@index([tenantId, type])
  @@index([tenantId, driverId])
  @@map("fleet_vehicles")
}

model Stop {
  id                         String                    @id @default(cuid())
  tenantId                   String
  tripId                     String?
  sequence                   Int?
  type                       StopType
  addressLine1               String
  addressLine2               String?
  city                       String
  postalCode                 String
  country                    String
  plannedAt                  DateTime?
  createdAt                  DateTime                  @default(now())
  updatedAt                  DateTime                  @updatedAt
  transportOrderId           String?
  completedAt                DateTime?
  startedAt                  DateTime?
  status                     StopStatus                @default(Pending)
  driver_wallet_transactions DriverWalletTransaction[]
  podPhotoDocuments          PodPhotoDocument[]
  pods                       Pod[]
  inventory_units            inventory_units[]
  tenant                     Tenant                    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  transportOrder             TransportOrder?           @relation("OrderStops", fields: [transportOrderId], references: [id])
  trip                       Trip?                     @relation(fields: [tripId], references: [id], onDelete: Cascade)
  lat                        Float?
  lng                        Float?
  placeId                    String?
  geocodedAt                 DateTime?

  @@index([tenantId, transportOrderId])
  @@index([tenantId, tripId])
  @@map("stops")
}

model Pod {
  id        String    @id @default(cuid())
  tenantId  String
  stopId    String
  status    PodStatus @default(Pending)
  signedBy  String?
  signedAt  DateTime?
  photoUrl  String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  photoKeys Json?
  stop      Stop      @relation(fields: [stopId], references: [id], onDelete: Cascade)
  tenant    Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId, stopId])
  @@index([tenantId, status])
  @@map("pods")
}

model PodPhotoDocument {
  id        String   @id @default(cuid())
  tenantId  String
  stopId    String
  photoKey  String
  createdAt DateTime @default(now())
  stop      Stop     @relation(fields: [stopId], references: [id], onDelete: Cascade)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId, stopId])
  @@map("pod_photo_documents")
}

model DriverWalletTransaction {
  id               String          @id @default(cuid())
  tenantId         String
  tripId           String?
  amountCents      Int
  currency         String          @default("SGD")
  type             String
  description      String?
  createdAt        DateTime        @default(now())
  driverId         String
  stopId           String?
  transportOrderId String?
  drivers          drivers         @relation(fields: [driverId], references: [id], onDelete: Cascade)
  stops            Stop?           @relation(fields: [stopId], references: [id])
  tenant           Tenant          @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  transport_orders TransportOrder? @relation(fields: [transportOrderId], references: [id])
  trip             Trip?           @relation(fields: [tripId], references: [id])

  @@unique([tripId, transportOrderId])
  @@index([stopId])
  @@index([tenantId, driverId])
  @@map("driver_wallet_transactions")
}

model EventLog {
  id         String   @id @default(cuid())
  tenantId   String
  entityType String
  entityId   String
  eventType  String
  payload    Json?
  createdAt  DateTime @default(now())
  tenant     Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId, entityType, entityId])
  @@index([tenantId, createdAt])
  @@map("event_logs")
}

model DriverLocationLatest {
  id           String   @id @default(cuid())
  tenantId     String
  driverUserId String
  lat          Float
  lng          Float
  accuracy     Float?
  heading      Float?
  speed        Float?
  recordedAt   DateTime?
  capturedAt   DateTime @default(now())
  lastMovedAt  DateTime?
  lastMovedLat Float?
  lastMovedLng Float?
  updatedAt    DateTime @updatedAt
  tenant       Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, driverUserId])
  @@index([tenantId, updatedAt])
  @@map("driver_location_latest")
}

model driver_wallet_entries {
  id          String   @id
  tenantId    String
  driverId    String
  amountCents Int
  currency    String   @default("SGD")
  sourceType  String
  /// TODO(phase-2): verify downstream export dependencies before removal.
  sourceId    String
  createdAt   DateTime @default(now())
  drivers     drivers  @relation(fields: [driverId], references: [id], onDelete: Cascade)
  tenants     Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([driverId, createdAt])
  @@index([tenantId, driverId])
}

model drivers {
  id                         String                    @id
  tenantId                   String
  email                      String
  name                       String
  phone                      String
  /// Driver is authorised to enter PSA port facilities. Default false.
  hasPsaPortAccess           Boolean                   @default(false)
  assignedVehicleId           String?
  assignedFleetVehicleId      String?
  createdAt                  DateTime                  @default(now())
  updatedAt                  DateTime
  userId                     String?
  driver_wallet_entries      driver_wallet_entries[]
  driver_wallet_transactions DriverWalletTransaction[]
  tenants                    Tenant                    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  users                      User?                     @relation(fields: [userId], references: [id])
  trips                      Trip[]
  assignedVehicle             Vehicle?                  @relation(fields: [assignedVehicleId], references: [id], onDelete: SetNull)
  assignedFleetVehicle        FleetVehicle?             @relation(fields: [assignedFleetVehicleId], references: [id], onDelete: SetNull)
  gpsDevices                  GpsDevice[]
  gpsPositions                GpsPosition[]
  tripExpenses                TripExpense[]

  @@unique([tenantId, email])
  @@unique([tenantId, userId])
  @@index([tenantId])
  @@index([tenantId, assignedVehicleId])
  @@index([tenantId, assignedFleetVehicleId])
}

model Job {
  id                 String    @id @default(cuid())
  tenantId           String
  customerCompanyId  String
  /// Optional accepted CustomerQuotation that governs this job's commercial pricing.
  sourceCustomerQuotationId String?
  internalRef        String
  externalRef        String?
  /// Compatibility singular only. Null for canonical multi-type jobs; never invent first-of-array.
  jobType            JobType?
  /// COLLECTION only: EMPTY or LOADED; null for other job types.
  collectionType     CollectionType?
  status             JobStatus @default(ONGOING)
  notes              String?
  /// Job-level pickup reference (container-style jobs). Prefer over JobItem.pickupReference.
  pickupReference    String?
  /// Job-level description (distinct from LCL item descriptions and Job.notes).
  description        String?   @db.Text
  /// Requested customer pickup calendar instant (tenant-local day anchor when date-only).
  pickupDate         DateTime?
  /// Requested pickup time precision: null = legacy/unknown (do not reinterpret);
  /// true = explicit requested time; false = date-only (time not specified).
  pickupDateHasTime  Boolean?
  /// Requested customer delivery calendar instant (tenant-local day anchor when date-only).
  deliveryDate       DateTime?
  /// Requested delivery time precision: null = legacy/unknown (do not reinterpret);
  /// true = explicit requested time; false = date-only (time not specified).
  deliveryDateHasTime Boolean?
  pickupAddress1     String
  pickupAddress2     String?
  pickupPostal       String?
  pickupContactName  String?
  pickupContactPhone String?
  deliveryAddress1   String
  deliveryAddress2   String?
  deliveryPostal     String?
  receiverName       String
  receiverPhone      String
  /// RETURN intake: depot unknown; Draft Job/Trip allowed. Must be cleared before publish.
  returningDepotPending Boolean @default(false)
  /// Preserved TBA/source wording while pending — never used as deliveryAddress1.
  returningDepotPendingText String?
  /// Deprecated runtime field: use Trip.assignedDriverUserId/Trip.driverId for execution assignment.
  assignedDriverId   String?
  /// Deprecated runtime field: use Trip.vehicleId/Trip.fleetVehicleId for execution assignment.
  assignedVehicleId  String?
  /// Deprecated runtime field: use Trip.vehicleId/Trip.fleetVehicleId for execution assignment.
  assignedFleetVehicleId String?
  /// Deprecated runtime field: assignment timestamp should come from Trip.assignedAt.
  assignedAt         DateTime?
  /// Deprecated runtime field: execution lifecycle is trip-driven.
  startedAt          DateTime?
  /// Deprecated runtime field: execution lifecycle is trip-driven.
  completedAt        DateTime?
  /// Deprecated runtime field: execution lifecycle is trip-driven.
  deliveredAt        DateTime?
  /// Set when ops explicitly hands this job over to finance invoicing flow.
  invoiceReadyAt     DateTime?
  /// Deprecated runtime field: derive proof/recipient data from TripDocument artifacts.
  podRecipientName   String?
  cancelledReason    String?
  cancelledAt        DateTime?
  cancelledByUserId  String?
  /// Deprecated runtime field: use DriverLocationLatest as location source of truth.
  lastLat            Float?
  /// Deprecated runtime field: use DriverLocationLatest as location source of truth.
  lastLng            Float?
  /// Deprecated runtime field: use DriverLocationLatest as location source of truth.
  lastLocationAt     DateTime?
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt

  createdByUserId String?
  /// IMPORT: Singapore port master code (master_singapore_ports.code)
  pickupPortCode  String?
  portTerminalCode String?
  portName         String?
  psaStorageRentLastDay DateTime?
  vesselName       String?
  vesselEta        DateTime?
  /// Optional shipping refs for all job types (IMPORT/EXPORT/COLLECTION/LCL).
  carrierName      String?
  voyage           String?
  shipper          String?
  portnetReady     Boolean  @default(false)
  permitReady      Boolean  @default(false)
  returningDepotCode   String?
  returnLastDay        DateTime?
  /// EXPORT: origin depot and destination port codes (master data)
  exportOriginDepotCode String?
  exportPortCode        String?

  tenant          Tenant             @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customerCompany customer_companies @relation("JobCustomerCompany", fields: [customerCompanyId], references: [id], onDelete: Restrict)
  sourceCustomerQuotation CustomerQuotation? @relation("JobSourceCustomerQuotation", fields: [sourceCustomerQuotationId], references: [id], onDelete: SetNull)
  assignedDriver  User?              @relation("JobAssignedDriver", fields: [assignedDriverId], references: [id], onDelete: SetNull)
  assignedFleetVehicle FleetVehicle? @relation(fields: [assignedFleetVehicleId], references: [id], onDelete: SetNull)
  createdBy       User?              @relation("JobCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  documents       JobDocument[]
  items           JobItem[]
  trips           Trip[]
  charges         JobCharge[]
  tripExpenses    TripExpense[]
  jobTypeAssignments JobTypeAssignment[]

  @@unique([tenantId, internalRef])
  /// Enables composite tenant-matched FKs (e.g. job_type_assignments).
  @@unique([tenantId, id])
  @@index([tenantId, customerCompanyId])
  @@index([tenantId, sourceCustomerQuotationId])
  @@index([tenantId, status])
  @@index([tenantId, pickupDate])
  @@index([tenantId, assignedDriverId])
  @@index([tenantId, assignedFleetVehicleId])
  @@index([tenantId, createdByUserId])
  @@index([tenantId, createdAt])
  @@index([tenantId, updatedAt])
  @@map("jobs")
}

model JobItem {
  id       String @id @default(cuid())
  tenantId String
  jobId    String

  /// Container number / LCL code. Null for Collection cargo until the driver enters identity.
  itemCode         String?
  description      String?
  sealNo           String?
  /// ISO-style container size for IMPORT/EXPORT/COLLECTION cargo. Nullable for legacy rows.
  containerSize    ContainerSize?
  pickupReference  String?
  qty              Int?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  job    Job    @relation(fields: [jobId], references: [id], onDelete: Cascade)
  tripDocuments TripDocument[]
  tripJobItems  TripJobItem[]

  @@index([tenantId, jobId])
  @@index([tenantId, itemCode])
  @@map("job_items")
}

enum JobMessageImportBatchStatus {
  IN_REVIEW
  CONFIRMED
  CANCELLED
}

enum JobMessageImportSourceChannel {
  WHATSAPP
}

enum JobMessageImportParserErrorSeverity {
  INFO
  WARNING
}

enum JobMessageImportDraftValidationStatus {
  READY
  NEEDS_REVIEW
  POSSIBLE_DUPLICATE
}

enum JobMessageImportDraftInclusionState {
  INCLUDED
  EXCLUDED
}

enum JobMessageImportMovementType {
  COLLECTION
  IMPORT
  EXPORT
  LCL
  RETURN
  ONE_WAY
  UNKNOWN
}

model JobMessageImportBatch {
  id              String                      @id @default(cuid())
  tenantId        String
  createdByUserId String?
  status          JobMessageImportBatchStatus @default(IN_REVIEW)

  sourceChannel JobMessageImportSourceChannel
  serviceDate   DateTime?
  timezone      String

  /// Server-side fingerprint for idempotent batch creation.
  /// Must not store secrets; derived from access-controlled source text + metadata.
  sourceFingerprint String @db.Text

  parserVersion   String
  modelName       String?
  parserVersionNo Int? // reserved for future migration-friendly versioning

  /// Parser warnings that may apply to the whole batch.
  batchWarningsJson Json?

  /// Safe parse metadata only (token usage, provider request id). Never raw payloads.
  parseMetadataJson Json?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  confirmedAt       DateTime?
  confirmedByUserId String?
  /// Monotonic version for optimistic concurrency on review/resume/edit operations.
  version           Int       @default(1)

  drafts JobMessageImportDraft[]

  /// Non-unique lookup index. Concurrent IN_REVIEW uniqueness is a partial unique
  /// index in SQL: (tenantId, sourceFingerprint) WHERE status = 'IN_REVIEW'.
  /// Prisma cannot express partial unique indexes; do not restore a permanent
  /// @@unique([tenantId, sourceFingerprint]).
  @@index([tenantId, sourceFingerprint])
  @@index([tenantId, status])
  @@index([tenantId, createdAt])
  @@map("job_message_import_batches")
}

model JobMessageImportDraft {
  id       String @id @default(cuid())
  tenantId String
  batchId  String

  /// Stable client-side draft identifier for review/edit operations.
  clientDraftId String

  movementType JobMessageImportMovementType

  /// Tenant-scoped source fragment to show in controller UI.
  sourceFragment String @db.Text

  /// Strong-ish fingerprint to prevent duplicates, also used for duplicate candidate lookups.
  duplicateFingerprint String @db.Text

  /// Classification / eligibility state for review UI.
  validationStatus JobMessageImportDraftValidationStatus @default(NEEDS_REVIEW)
  inclusionState   JobMessageImportDraftInclusionState   @default(INCLUDED)

  /// Stored parsing + controller-edited payloads (untrusted, validated server-side).
  parsedJson     Json
  controllerJson Json?

  fieldEvidenceJson Json?
  draftWarningsJson Json?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  version           Int       @default(1)
  confirmedAt       DateTime?
  confirmedByUserId String?

  /// Link to the canonical Job created when confirmed (null until confirmed).
  canonicalJobId String?

  /// When a POSSIBLE_DUPLICATE draft is confirmed with override, record the override.
  duplicateOverrideReason      String?
  duplicateOverrideActorUserId String?
  duplicateOverrideAt          DateTime?

  batch JobMessageImportBatch @relation(fields: [batchId], references: [id], onDelete: Cascade)

  @@unique([tenantId, batchId, clientDraftId])
  @@index([tenantId, batchId, validationStatus])
  @@index([tenantId, batchId, inclusionState])
  @@index([tenantId, duplicateFingerprint])
  @@map("job_message_import_drafts")
}

/// Canonical trip ↔ job-item (container) M:N link. Sole source of truth for cargo scope,
/// documentation, completion, and publish validation. Every row is an explicit verified link.
model TripJobItem {
  id                       String   @id @default(cuid())
  tenantId                 String
  tripId                   String
  jobItemId                String
  containerNumberSnapshot  String?
  linkedAt                 DateTime @default(now())
  linkedByUserId           String?
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt

  tenant   Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  trip     Trip     @relation(fields: [tripId], references: [id], onDelete: Cascade)
  jobItem  JobItem  @relation(fields: [jobItemId], references: [id], onDelete: Cascade)
  linkedBy User?    @relation("TripJobItemLinkedBy", fields: [linkedByUserId], references: [id], onDelete: SetNull)

  @@unique([tenantId, tripId, jobItemId])
  @@index([tenantId, tripId])
  @@index([tenantId, jobItemId])
  @@index([tripId])
  @@map("trip_job_items")
}

model JobDocument {
  id               String          @id @default(cuid())
  tenantId         String
  jobId            String
  type             JobDocumentType
  storageKey       String
  originalName     String
  mimeType         String
  sizeBytes        Int?
  uploadedByUserId String?
  isActive         Boolean         @default(true)
  uploadedByNameSnapshot String?
  createdAt        DateTime        @default(now())
  updatedAt        DateTime        @updatedAt

  job        Job    @relation(fields: [jobId], references: [id], onDelete: Cascade)
  tenant     Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  uploadedBy User?  @relation("JobDocumentUploadedBy", fields: [uploadedByUserId], references: [id], onDelete: SetNull)

  @@index([tenantId, jobId])
  @@index([tenantId, type])
  @@map("job_documents")
}

/// Company-level quotation file versions (Supabase storage). One ACTIVE per company at a time.
model CustomerCompanyQuotation {
  id                String                 @id @default(cuid())
  tenantId          String
  customerCompanyId String
  storageKey        String
  originalName      String
  mimeType          String
  sizeBytes         Int?
  uploadedByUserId  String?
  effectiveDate     DateTime?
  status            QuotationVersionStatus @default(ACTIVE)
  parsedSummaryJson Json?
  createdAt         DateTime               @default(now())

  tenant          Tenant             @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customerCompany customer_companies @relation(fields: [customerCompanyId], references: [id], onDelete: Cascade)
  uploadedBy      User?              @relation("QuotationUploadedBy", fields: [uploadedByUserId], references: [id], onDelete: SetNull)
  rateLines       CustomerQuotationRateLine[]

  @@index([tenantId, customerCompanyId])
  @@index([tenantId, customerCompanyId, status])
  @@map("customer_company_quotations")
}

model CustomerCompanyDocument {
  id                String                        @id @default(cuid())
  tenantId          String
  customerCompanyId String
  type              CustomerCompanyDocumentType   @default(CUSTOMER_DOCUMENT)
  fileName          String
  storageKey        String
  fileUrl           String
  mimeType          String
  fileSizeBytes     Int?
  uploadedByUserId  String?
  generatedByUserId String?
  uploadedAt        DateTime                      @default(now())
  generatedAt       DateTime?
  sourceJobId       String?
  sourceInvoiceId   String?
  status            CustomerCompanyDocumentStatus @default(ACTIVE)
  deletedAt         DateTime?
  createdAt         DateTime                      @default(now())
  updatedAt         DateTime                      @updatedAt

  tenant          Tenant             @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customerCompany customer_companies @relation(fields: [customerCompanyId], references: [id], onDelete: Cascade)
  uploadedBy      User?              @relation("CustomerCompanyDocumentUploadedBy", fields: [uploadedByUserId], references: [id], onDelete: SetNull)
  generatedBy     User?              @relation("CustomerCompanyDocumentGeneratedBy", fields: [generatedByUserId], references: [id], onDelete: SetNull)

  @@index([tenantId, customerCompanyId, status, uploadedAt])
  @@index([tenantId, customerCompanyId, type])
  @@map("customer_company_documents")
}

model CustomerQuotationRateLine {
  id           String   @id @default(cuid())
  tenantId     String
  quotationId  String
  section      String?
  code         String
  label        String
  description  String?
  unit         String?
  rateCents    Int?
  requiresManualAmount Boolean @default(false)
  rawRateText  String?
  containerSize String?
  tripMode     String?
  areaScope    String?
  sortOrder    Int      @default(0)
  /// PARSER_ANNEX | MANUAL | SEED
  sourceType   String   @default("PARSER_ANNEX")
  createdAt    DateTime @default(now())

  tenant    Tenant                   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  quotation CustomerCompanyQuotation @relation(fields: [quotationId], references: [id], onDelete: Cascade)

  @@index([tenantId, quotationId])
  @@index([tenantId, code])
  @@map("customer_quotation_rate_lines")
}

/// Canonical structured rate master (Excel-controlled) used for job charge/rate selection.
model CustomerRateMasterLine {
  id            String   @id @default(cuid())
  tenantId      String
  customerCompanyId String
  section       String?
  code          String
  label         String
  description   String?
  category      String?
  containerSize String?
  tripMode      String?
  areaScope     String?
  unit          String?
  rateCents     Int?
  requiresManualAmount Boolean @default(false)
  rawRateText   String?
  notes         String?
  isSelectableForJob         Boolean @default(true)
  isSelectableForTripEarning Boolean @default(false)
  active        Boolean  @default(true)
  sortOrder     Int      @default(0)
  sourceType    String   @default("EXCEL_MASTER")
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  tenant          Tenant             @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customerCompany customer_companies @relation(fields: [customerCompanyId], references: [id], onDelete: Cascade)

  @@index([tenantId, customerCompanyId, active])
  @@index([tenantId, customerCompanyId, code])
  @@map("customer_rate_master_lines")
}

/// Frozen billable lines selected for a job (never updated from live quotation/master).
model JobCharge {
  id                String             @id @default(cuid())
  tenantId          String
  jobId             String
  sourceType        JobChargeSourceType
  sourceRefId       String?
  sourceCustomerQuotationItemId String?
  /// Provenance to a CustomerQuotationLine. Distinct from legacy sourceCustomerQuotationItemId.
  sourceCustomerQuotationLineId String?
  code              String
  label             String
  description       String?
  qty               Int                @default(1)
  unitPriceCents    Int
  amountCents       Int
  currency          String             @default("SGD")
  taxable           Boolean            @default(true)
  taxCode           String?
  taxRateBasisPoints Int?
  sortOrder         Int                @default(0)
  selectedByUserId  String?
  overrideReason    String?
  metadataJson      Json?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt

  tenant       Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  job          Job    @relation(fields: [jobId], references: [id], onDelete: Cascade)
  sourceCustomerQuotationLine CustomerQuotationLine? @relation("JobChargeSourceCustomerQuotationLine", fields: [sourceCustomerQuotationLineId], references: [id], onDelete: SetNull)
  selectedBy   User?  @relation("JobChargeSelectedBy", fields: [selectedByUserId], references: [id], onDelete: SetNull)
  invoiceLineItems InvoiceLineItem[] @relation("InvoiceLineItemJobCharge")
  invoiceChargeReservations InvoiceChargeReservation[]

  @@index([tenantId, jobId])
  @@index([tenantId, sourceCustomerQuotationItemId])
  @@index([tenantId, sourceCustomerQuotationLineId])
  @@map("job_charges")
}

/// Tenant-scoped DHC / depot handling lookup rows for charge selection (snapshots go to JobCharge).
model DepotHandlingReference {
  id           String   @id @default(cuid())
  tenantId     String
  code         String
  label        String
  amountCents  Int?
  currency     String   @default("SGD")
  active       Boolean  @default(true)
  rawRateText  String?
  requiresManualAmount Boolean @default(false)
  hasMultipleRates Boolean @default(false)
  rateOptionsJson Json?
  defaultRateOptionIndex Int?
  metadataJson Json?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, code])
  @@index([tenantId, active])
  @@map("depot_handling_references")
}

/// Trucking rate master copy (per tenant). Trip stores snapshot on assignment.
model DriverTripRateMaster {
  id           String   @id @default(cuid())
  tenantId     String
  code         String
  label        String
  amountCents  Int?
  currency     String   @default("SGD")
  active       Boolean  @default(true)
  rawRateText  String?
  requiresManualAmount Boolean @default(false)
  hasMultipleRates Boolean @default(false)
  rateOptionsJson Json?
  defaultRateOptionIndex Int?
  metadataJson Json?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  trips  Trip[]

  @@unique([tenantId, code])
  @@index([tenantId, active])
  @@map("driver_trip_rate_masters")
}

model TripDocument {
  id               String           @id @default(cuid())
  tenantId         String
  tripId           String
  /// Container/seal photos link to the exact current JobItem container row.
  /// Null for trip-level documents such as DO, POD, and trailer photos.
  jobItemId        String?
  type             TripDocumentType
  storageKey       String
  originalName     String
  mimeType         String
  sizeBytes        Int?
  uploadedByUserId String?
  isActive         Boolean          @default(true)
  uploadedByNameSnapshot String?
  generatedBySystem Boolean          @default(false)
  generatedSource   String?
  requiresSignature Boolean          @default(false)
  isSigned          Boolean          @default(false)
  signedAt          DateTime?
  signedByUserId    String?
  signedByName      String?
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt

  tenant     Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  trip       Trip   @relation(fields: [tripId], references: [id], onDelete: Cascade)
  /// Set null when an Ops item row is removed so the document remains for audit/history.
  jobItem    JobItem? @relation(fields: [jobItemId], references: [id], onDelete: SetNull)
  uploadedBy User?  @relation("TripDocumentUploadedBy", fields: [uploadedByUserId], references: [id], onDelete: SetNull)

  @@index([tenantId, tripId])
  @@index([tenantId, type])
  @@index([tenantId, tripId, isActive, type])
  @@index([tenantId, tripId, jobItemId, type])
  @@map("trip_documents")
}

model TripPayoutLine {
  id                        String             @id @default(cuid())
  tenantId                  String
  tripId                    String
  sourceType                JobChargeSourceType
  payoutItemId              String?
  earningRateMasterId       String?
  code                      String?
  label                     String
  description               String?
  unit                      String?
  quantity                  Int                @default(1)
  amountCents               Int?
  totalCents                Int?
  notes                     String?
  isManual                  Boolean            @default(false)
  requiresManualAmount      Boolean            @default(false)
  isSelectableForTripEarning Boolean           @default(true)
  sortOrder                 Int                @default(0)
  createdAt                 DateTime           @default(now())
  updatedAt                 DateTime           @updatedAt

  tenant            Tenant               @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  trip              Trip                 @relation(fields: [tripId], references: [id], onDelete: Cascade)
  payoutItem        DriverPayoutItem?    @relation(fields: [payoutItemId], references: [id], onDelete: SetNull)
  earningRateMaster MasterRateDatasetRow? @relation(fields: [earningRateMasterId], references: [id], onDelete: SetNull)

  @@index([tenantId, tripId])
  @@index([tenantId, payoutItemId])
  @@index([tenantId, earningRateMasterId])
  @@map("trip_payout_lines")
}

model TripDocumentRequirement {
  id                  String                         @id @default(cuid())
  tenantId            String
  tripId              String
  type                TripDocumentType
  label               String
  isRequired          Boolean                        @default(true)
  requiresSignature   Boolean                        @default(false)
  minCount            Int                            @default(1)
  sortOrder           Int                            @default(0)
  /// Who is expected to upload this requirement artifact.
  responsibleUploader TripDocumentResponsibleUploader @default(DRIVER)
  /// Lifecycle gate this requirement participates in.
  requirementStage    TripDocumentRequirementStage    @default(BEFORE_COMPLETE)
  createdAt           DateTime                       @default(now())
  updatedAt           DateTime                       @updatedAt

  trip   Trip   @relation(fields: [tripId], references: [id], onDelete: Cascade)
  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, tripId, type, requirementStage], map: "trip_document_requirements_tenant_trip_type_stage_key")
  @@index([tenantId, tripId])
  @@map("trip_document_requirements")
}

/// Global Singapore ports list (not tenant-scoped; extend via migrations or admin later).
model MasterSingaporePort {
  id           String  @id @default(cuid())
  code         String  @unique
  name         String
  addressLine1 String?
  addressLine2 String?
  postalCode   String?
  country      String? @default("SG")
  lat          Float?
  lng          Float?
  placeId      String?

  @@map("master_singapore_ports")
}

model MasterSingaporeDepot {
  id                     String  @id @default(cuid())
  code                   String  @unique
  name                   String
  addressLine1           String?
  addressLine2           String?
  postalCode             String?
  country                String? @default("SG")
  lat                    Float?
  lng                    Float?
  placeId                String?
  /// Human-readable display summary from Portnet/operator directories (not a scheduling engine).
  operatingHoursSummary  String?

  @@map("master_singapore_depots")
}

/// Controlled list: 7 Gul Circle trailer last-known locations (extend later).
model MasterTrailerLocation {
  id   String @id @default(cuid())
  code String @unique
  name String

  @@map("master_trailer_locations")
}

model AuditLog {
  id          String   @id @default(cuid())
  tenantId    String
  actorUserId String?
  entityType  String
  entityId    String
  action      String
  metadata    Json?
  createdAt   DateTime @default(now())

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId, entityType, entityId])
  @@index([tenantId, createdAt])
  @@map("audit_logs")
}

model job_internal_ref_counters {
  tenantId  String
  yyyymm    String
  nextSeq   Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@id([tenantId, yyyymm])
  @@index([tenantId])
  @@map("job_internal_ref_counters")
}

model warehouse_job_internal_ref_counters {
  tenantId  String
  yyyymm    String
  nextSeq   Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@id([tenantId, yyyymm])
  @@index([tenantId])
  @@map("warehouse_job_internal_ref_counters")
}

model warehouse_job_customer_ref_counters {
  tenantId        String
  yy              String
  customerInitial String
  nextSeq         Int
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@id([tenantId, yy, customerInitial])
  @@index([tenantId])
  @@map("warehouse_job_customer_ref_counters")
}

model inventory_items {
  id                    String                  @id
  tenantId              String
  sku                   String
  name                  String
  reference             String?
  availableQty          Int                     @default(0)
  createdAt             DateTime                @default(now())
  updatedAt             DateTime
  tenants               Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  inventory_units       inventory_units[]
  inventory_batch_items inventory_batch_items[]
  transport_order_items transport_order_items[]
  warehouseJobLines     WarehouseJobLine[]

  @@unique([tenantId, sku])
  @@index([tenantId])
}

model profiles {
  id         String   @id
  userId     String   @unique
  createdAt  DateTime @default(now())
  updatedAt  DateTime
  users      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

/// Legacy TenantMembership.role values. Transitional compatibility only.
/// New authorization writes go to TenantMembershipRole + CanonicalTenantRole.
enum Role {
  ADMIN
  // Deprecated compatibility alias for TRANSPORT_STAFF — do not assign to new memberships.
  OPS
  TRANSPORT_STAFF
  FINANCE
  DRIVER
  CUSTOMER
  WAREHOUSE
}

/// Canonical tenant RBAC roles stored on TenantMembershipRole.
enum CanonicalTenantRole {
  TENANT_ADMIN
  TRANSPORT_ADMIN
  TRANSPORT_DRIVER
  FINANCE_ADMIN
  WAREHOUSE_ADMIN
  WAREHOUSE_STAFF
  CUSTOMER_ADMIN
}

enum MembershipStatus {
  Active
  Invited
  Suspended
}

enum OrderStatus {
  Open
  Confirmed
  Planned
  Dispatched
  InTransit
  Delivered
  Closed
  Cancelled
}

enum TripStatus {
  DRAFT
  PUBLISHED
  ONGOING
  COMPLETED
  DONE
  CANCELLED
}

enum TripPendingState {
  NONE
  PENDING_AT_PORT
  PENDING_AT_DEPOT
}

enum StopType {
  PICKUP
  DELIVERY
}

enum PodStatus {
  Pending
  Completed
  Failed
}

enum StopStatus {
  Pending
  InProgress
  Completed
}

enum DoStatus {
  PENDING
  SIGNED
}

enum UserRole {
  SUPERADMIN
  USER
}

/// Tenant lifecycle for platform management (additive; existing rows default ACTIVE).
enum TenantStatus {
  SETUP
  ACTIVE
  SUSPENDED
  ARCHIVED
}

/// Product modules a tenant may be entitled to.
enum TenantModule {
  TRANSPORT
  WAREHOUSING
  FINANCE
}

enum PlatformAdminStatus {
  ACTIVE
  DISABLED
}

/// Platform Super Admin identity — NOT a TenantMembership / tenant Role.
model PlatformAdmin {
  id              String               @id @default(cuid())
  userId          String               @unique
  status          PlatformAdminStatus  @default(ACTIVE)
  createdAt       DateTime             @default(now())
  updatedAt       DateTime             @updatedAt
  createdByUserId String?
  notes           String?

  user      User               @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdBy User?              @relation("PlatformAdminCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  auditLogs PlatformAuditLog[]

  @@index([status])
  @@map("platform_admins")
}

model TenantModuleEntitlement {
  id        String       @id @default(cuid())
  tenantId  String
  module    TenantModule
  enabled   Boolean      @default(true)
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, module])
  @@index([tenantId])
  @@map("tenant_module_entitlements")
}

/// Append-only platform action log. Do not update/delete rows in application code.
model PlatformAuditLog {
  id                   String   @id @default(cuid())
  actorPlatformAdminId String
  actorUserId          String
  targetTenantId       String?
  action               String
  entityType           String?
  entityId             String?
  correlationId        String?
  reason               String?
  /// Redacted metadata only — never store secrets/passwords.
  metadata             Json?
  createdAt            DateTime @default(now())

  actorPlatformAdmin PlatformAdmin @relation(fields: [actorPlatformAdminId], references: [id], onDelete: Restrict)
  actorUser          User          @relation("PlatformAuditActorUser", fields: [actorUserId], references: [id], onDelete: Restrict)
  targetTenant       Tenant?       @relation("PlatformAuditTargetTenant", fields: [targetTenantId], references: [id], onDelete: SetNull)

  @@index([createdAt])
  @@index([actorPlatformAdminId, createdAt])
  @@index([targetTenantId, createdAt])
  @@index([action, createdAt])
  @@map("platform_audit_logs")
}

enum JobType {
  LCL
  IMPORT
  EXPORT
  COLLECTION
  /// Container return to depot (prime mover); one Trip pickup → depot.
  RETURN
  /// Container one-way haul (prime mover); one Trip pickup → delivery.
  ONE_WAY
}

/// Phase 4 canonical multi-value job types (tenant-bound).
/// Compatibility Job.jobType is nullable and must not classify multi-type jobs.
model JobTypeAssignment {
  id        String   @id @default(cuid())
  tenantId  String
  jobId     String
  jobType   JobType
  createdAt DateTime @default(now())

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  /// Composite FK enforces assignment.tenantId == job.tenantId (no cross-tenant jobId).
  job    Job    @relation(fields: [tenantId, jobId], references: [tenantId, id], onDelete: Cascade)

  @@unique([tenantId, jobId, jobType])
  @@index([tenantId, jobId])
  @@index([tenantId, jobType])
  @@map("job_type_assignments")
}

enum CollectionType {
  EMPTY
  LOADED
}

/// JobItem container size for IMPORT/EXPORT/COLLECTION cargo lines.
/// Wire/API values are mapped: FT_20 → "20ft", FT_40 → "40ft", FT_45 → "45ft".
enum ContainerSize {
  FT_20 @map("20ft")
  FT_40 @map("40ft")
  FT_45 @map("45ft")
}

enum JobStatus {
  ONGOING
  READY_FOR_INVOICE
  COMPLETED
  CANCELLED
}

enum InvoiceStatus {
  DRAFT
  GENERATED
  ISSUED
  PAID
  VOID
}

enum JobDocumentType {
  QUOTATION
  OTHER
}

enum JobTripTemplate {
  PICKUP_TO_DELIVERY
  DELIVERY_TO_DEPOT
  DEPOT_TO_DELIVERY
  DELIVERY_TO_PORT
  PORT_TO_DEPOT
  CUSTOMER_TO_GUL
  GUL_TO_CUSTOMER
  CUSTOM
}

enum QuotationVersionStatus {
  ACTIVE
  SUPERSEDED
}

enum CustomerCompanyDocumentType {
  CUSTOMER_DOCUMENT
  INVOICE
  COMPANY_INVOICE
}

enum CustomerCompanyDocumentStatus {
  ACTIVE
  DELETED
}

enum MasterFileType {
  QUOTATION
  CUSTOMER_QUOTATION
  DRIVER_PAYOUT
  DHC_REFERENCE
}

enum MasterFileStatus {
  PARSED
  PARSE_FAILED
  SUPERSEDED
}

enum MasterRateDatasetType {
  QUOTATION
  TRUCKING_RATES
  DHC_RATES
}

enum MasterRateDatasetStatus {
  DRAFT
  ACTIVE
}

enum JobChargeSourceType {
  CUSTOMER_QUOTATION
  DRIVER_RATE_MASTER
  DHC_REFERENCE
  MANUAL
}

enum TripDocumentType {
  PICKUP_DO
  DELIVERY_DO
  /// Unsigned/signed lorry chit PDF generated at trip create when selected.
  LORRY_CHIT
  POD_PHOTO
  POD_SIGNATURE
  /// Handwritten signature image captured when signing Pickup DO.
  PICKUP_SIGNATURE
  /// Handwritten signature image captured when signing Delivery DO.
  DELIVERY_SIGNATURE
  /// Handwritten signature image captured when signing Lorry Chit.
  LORRY_CHIT_SIGNATURE
  OTHER
  /// Operations/driver port or entry permit (trip-scoped).
  PERMIT
  /// Driver-uploaded container number photo (required before trip completion).
  CONTAINER_PHOTO
  /// Driver-uploaded seal number photo (required before trip completion).
  SEAL_PHOTO
  /// legacy values kept for backward compatibility with existing rows
  SIGNATURE
  OFFLOADING
  OFFLOADING_PHOTO
  TRAILER_PARKING_PHOTO
  TRAILER_START_PHOTO
  TRAILER_END_PHOTO
}

/// Who must provide a trip document requirement artifact.
enum TripDocumentResponsibleUploader {
  DRIVER
  OPERATIONS
  EITHER
}

/// When a trip document requirement blocks the lifecycle.
enum TripDocumentRequirementStage {
  BEFORE_DISPATCH
  BEFORE_START
  BEFORE_COMPLETE
  REFERENCE_ONLY
}

enum LogisticsLocationType {
  PORT
  DEPOT
}

enum VehicleType {
  VAN
  TRUCK_10FT
  TRUCK_14FT
  TRUCK_24FT
  TRAILER
  PRIME_MOVER
  LORRY_CRANE
  MOTORCYCLE
}

enum VehicleStatus {
  ACTIVE
  MAINTENANCE
  INACTIVE
}

enum InventoryBatchStatus {
  Draft
  Open
  Completed
  Cancelled
}

enum InventoryUnitStatus {
  Available
  Reserved
  Dispatched
  InTransit
  Delivered
  Returned
  Damaged
  Cancelled
}

enum WarehouseJobType {
  RECEIVE
  PUTAWAY
  PICK
  PACK
  STOCK_ADJUSTMENT
  RETURN_PROCESSING
  INTERNAL_MOVE
  CYCLE_COUNT
  STUFFING
  UNSTUFFING
  LASHING_CHOKING
  PALLETIZING
  PACKAGING
  CRATING
  STORAGE
  PICK_AND_PACK
}

enum WarehouseJobStatus {
  DRAFT
  OPEN
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum WarehouseJobPriority {
  LOW
  NORMAL
  HIGH
  URGENT
}

enum WarehouseJobUnitLinkStatus {
  PLANNED
  CONFIRMED
  RELEASED
}

enum WarehouseJobEventType {
  CREATED
  STATUS_CHANGED
  LINE_ADDED
  LINE_UPDATED
  LINE_REMOVED
  UNIT_LINKED
  UNIT_CONFIRMED
  UNIT_RELEASED
  ASSIGNED
  NOTE_ADDED
  CANCELLED
  COMPLETED
  DOCUMENT_UPLOADED
  DOCUMENT_UPDATED
  DOCUMENT_DELETED
  DOCUMENT_APPROVED
  DOCUMENT_REJECTED
  EXECUTION_UPDATED
}

enum WarehouseJobDocumentType {
  PACKING_LIST
  DELIVERY_ORDER
  INSTRUCTION
  REFERENCE_PHOTO
  WAREHOUSE_PHOTO
  DAMAGE_PHOTO
  COMPLETION_PHOTO
  OTHER
}

enum WarehouseJobDocumentSource {
  ADMIN
  OPS
  WAREHOUSE
}

enum WarehouseJobDocumentReviewStatus {
  PENDING_REVIEW
  APPROVED
  REJECTED
}

model inventory_batches {
  id              String @id @default(cuid())
  tenantId        String
  containerNumber String

  // ✅ Stock-In v2 fields
  batchDescription  String?
  customerCompanyId String?
  customerContactId String?

  // Legacy fields (kept for backward compatibility)
  customerName String?
  customerRef  String?

  receivedAt DateTime?
  notes      String?
  status     InventoryBatchStatus @default(Draft)
  createdAt  DateTime             @default(now())
  updatedAt  DateTime             @updatedAt

  tenant          Tenant              @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customerCompany customer_companies? @relation("InventoryBatchCustomerCompany", fields: [customerCompanyId], references: [id], onDelete: SetNull)
  customerContact customer_contacts?  @relation("InventoryBatchCustomerContact", fields: [customerContactId], references: [id], onDelete: SetNull)

  inventory_batch_items inventory_batch_items[]
  inventory_units       inventory_units[]
  transport_order_items transport_order_items[]
  warehouseJobs         WarehouseJob[]
  warehouseJobLines     WarehouseJobLine[]

  @@unique([tenantId, containerNumber])
  @@index([tenantId, status])
  @@index([tenantId, createdAt])
  @@index([tenantId, customerCompanyId])
  @@index([tenantId, customerContactId])
  @@map("inventory_batches")
}

model inventory_batch_items {
  id              String            @id @default(cuid())
  tenantId        String
  batchId         String
  inventoryItemId String
  qty             Int
  createdAt       DateTime          @default(now())
  updatedAt       DateTime          @updatedAt
  tenant          Tenant            @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  batch           inventory_batches @relation(fields: [batchId], references: [id], onDelete: Cascade)
  inventory_item  inventory_items   @relation(fields: [inventoryItemId], references: [id], onDelete: Cascade)

  @@unique([batchId, inventoryItemId])
  @@index([tenantId, batchId])
  @@map("inventory_batch_items")
}

model inventory_units {
  id               String                       @id @default(cuid())
  tenantId         String
  inventoryItemId  String
  batchId          String
  unitSku          String
  status           InventoryUnitStatus          @default(Available)
  transportOrderId String?
  tripId           String?
  stopId           String?
  createdAt        DateTime                     @default(now())
  updatedAt        DateTime                     @updatedAt
  tenant           Tenant                       @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  inventory_item   inventory_items              @relation(fields: [inventoryItemId], references: [id], onDelete: Cascade)
  batch            inventory_batches            @relation(fields: [batchId], references: [id], onDelete: Cascade)
  transport_order  TransportOrder?              @relation(fields: [transportOrderId], references: [id], onDelete: SetNull)
  trip             Trip?                        @relation(fields: [tripId], references: [id], onDelete: SetNull)
  stop             Stop?                        @relation(fields: [stopId], references: [id], onDelete: SetNull)
  order_item_links transport_order_item_units[]
  warehouseJobUnits WarehouseJobUnit[]

  @@unique([tenantId, unitSku])
  @@index([tenantId, inventoryItemId, status])
  @@index([tenantId, batchId])
  @@index([tenantId, transportOrderId])
  @@index([tenantId, status])
  @@map("inventory_units")
}

model WarehouseJob {
  id                String               @id @default(cuid())
  tenantId          String
  internalRef       String
  type              WarehouseJobType
  status            WarehouseJobStatus   @default(DRAFT)
  priority          WarehouseJobPriority @default(NORMAL)
  title             String?
  description       String?
  notes             String?
  customerCompanyId String?
  inventoryBatchId  String?
  assignedToUserId  String?
  /// CS / transport staff person in charge (office).
  csInChargeUserId  String?
  createdByUserId   String?
  scheduledAt       DateTime?
  startedAt         DateTime?
  completedAt       DateTime?
  cancelledAt       DateTime?
  cancelledReason   String?
  containerNumber   String?
  sealNumber        String?
  warehouseNotes    String?
  customerReference String?
  orderReference  String?
  customerInitial String?
  creatorInitial  String?
  customerReferenceSeq Int?
  receivingVessel String?
  placeOfDelivery String?
  destinationCountry String? @default("Singapore")
  arrivalDate     DateTime?
  departureDate   DateTime?
  generateDeliveryOrder Boolean @default(false)
  deliveryOrderGeneratedAt DateTime?
  /// Points to WarehouseJobDocument.id (no FK to avoid circular relation complexity).
  deliveryOrderDocumentId String?
  /// Opaque cross-domain pointer (e.g. TRANSPORT_ORDER + id) — no FK to transport tables.
  externalRefType   String?
  externalRefId     String?
  createdAt         DateTime             @default(now())
  updatedAt         DateTime             @updatedAt

  tenant          Tenant              @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customerCompany customer_companies? @relation("WarehouseJobCustomerCompany", fields: [customerCompanyId], references: [id], onDelete: SetNull)
  inventoryBatch  inventory_batches?  @relation(fields: [inventoryBatchId], references: [id], onDelete: SetNull)
  assignedToUser  User?               @relation("WarehouseJobAssignedTo", fields: [assignedToUserId], references: [id], onDelete: SetNull)
  csInChargeUser  User?               @relation("WarehouseJobCsInCharge", fields: [csInChargeUserId], references: [id], onDelete: SetNull)
  createdByUser   User?               @relation("WarehouseJobCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)

  lines     WarehouseJobLine[]
  units     WarehouseJobUnit[]
  events    WarehouseJobEvent[]
  documents WarehouseJobDocument[]
  cargoLines WarehouseJobCargoLine[]
  containers WarehouseJobContainer[]

  @@unique([tenantId, internalRef])
  @@index([tenantId, status])
  @@index([tenantId, type])
  @@index([tenantId, priority])
  @@index([tenantId, customerCompanyId])
  @@index([tenantId, inventoryBatchId])
  @@index([tenantId, assignedToUserId])
  @@index([tenantId, csInChargeUserId])
  @@index([tenantId, createdAt])
  @@index([tenantId, externalRefType, externalRefId])
  @@map("warehouse_jobs")
}

model WarehouseJobLine {
  id               String   @id @default(cuid())
  tenantId         String
  warehouseJobId   String
  inventoryItemId  String?
  inventoryBatchId String?
  description      String?
  requestedQty     Int      @default(0)
  completedQty     Int      @default(0)
  sortOrder        Int      @default(0)
  notes            String?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  tenant         Tenant             @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  warehouseJob   WarehouseJob       @relation(fields: [warehouseJobId], references: [id], onDelete: Cascade)
  inventoryItem  inventory_items?   @relation(fields: [inventoryItemId], references: [id], onDelete: Restrict)
  inventoryBatch inventory_batches? @relation(fields: [inventoryBatchId], references: [id], onDelete: Restrict)

  units WarehouseJobUnit[]

  @@index([tenantId, warehouseJobId])
  @@index([tenantId, inventoryItemId])
  @@index([tenantId, inventoryBatchId])
  @@index([warehouseJobId, sortOrder])
  @@map("warehouse_job_lines")
}

model WarehouseJobUnit {
  id                 String                     @id @default(cuid())
  tenantId           String
  warehouseJobId     String
  warehouseJobLineId String?
  inventoryUnitId    String
  linkStatus         WarehouseJobUnitLinkStatus @default(PLANNED)
  scannedAt          DateTime?
  confirmedAt        DateTime?
  releasedAt         DateTime?
  createdAt          DateTime                   @default(now())
  updatedAt          DateTime                   @updatedAt

  tenant           Tenant            @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  warehouseJob     WarehouseJob      @relation(fields: [warehouseJobId], references: [id], onDelete: Cascade)
  warehouseJobLine WarehouseJobLine? @relation(fields: [warehouseJobLineId], references: [id], onDelete: SetNull)
  inventoryUnit    inventory_units   @relation(fields: [inventoryUnitId], references: [id], onDelete: Restrict)

  @@unique([tenantId, warehouseJobId, inventoryUnitId])
  @@index([tenantId, warehouseJobId])
  @@index([tenantId, warehouseJobLineId])
  @@index([tenantId, inventoryUnitId])
  @@index([tenantId, linkStatus])
  @@map("warehouse_job_units")
}

model WarehouseJobEvent {
  id             String                @id @default(cuid())
  tenantId       String
  warehouseJobId String
  actorUserId    String?
  eventType      WarehouseJobEventType
  fromStatus     WarehouseJobStatus?
  toStatus       WarehouseJobStatus?
  payload        Json?
  createdAt      DateTime              @default(now())

  tenant       Tenant       @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  warehouseJob WarehouseJob @relation(fields: [warehouseJobId], references: [id], onDelete: Cascade)
  actorUser    User?        @relation("WarehouseJobEventActor", fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([tenantId, warehouseJobId, createdAt])
  @@index([tenantId, eventType])
  @@index([tenantId, actorUserId])
  @@map("warehouse_job_events")
}

model WarehouseJobDocument {
  id               String                           @id @default(cuid())
  tenantId         String
  warehouseJobId   String
  uploadedByUserId String?
  type             WarehouseJobDocumentType
  source           WarehouseJobDocumentSource
  reviewStatus     WarehouseJobDocumentReviewStatus @default(PENDING_REVIEW)
  originalName     String?
  fileName         String?
  mimeType         String?
  sizeBytes        Int?
  storageKey       String
  url              String?
  notes            String?
  reviewedByUserId String?
  reviewedAt       DateTime?
  rejectedReason   String?
  createdAt        DateTime                         @default(now())
  updatedAt        DateTime                         @updatedAt

  tenant         Tenant       @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  warehouseJob   WarehouseJob @relation(fields: [warehouseJobId], references: [id], onDelete: Cascade)
  uploadedByUser User?        @relation("WarehouseJobDocumentUploadedBy", fields: [uploadedByUserId], references: [id], onDelete: SetNull)
  reviewedByUser User?        @relation("WarehouseJobDocumentReviewedBy", fields: [reviewedByUserId], references: [id], onDelete: SetNull)

  @@index([tenantId, warehouseJobId])
  @@index([tenantId, type])
  @@index([tenantId, source])
  @@index([tenantId, reviewStatus])
  @@index([tenantId, createdAt])
  @@map("warehouse_job_documents")
}

model WarehouseJobCargoLine {
  id             String   @id @default(cuid())
  tenantId       String
  warehouseJobId String
  description    String
  vesselName     String?
  poNumber       String?
  quantity       Int      @default(0)
  totalWeightKg  Decimal? @db.Decimal(12, 3)
  lengthCm       Decimal? @db.Decimal(10, 2)
  widthCm        Decimal? @db.Decimal(10, 2)
  heightCm       Decimal? @db.Decimal(10, 2)
  unitType       String?
  sortOrder      Int      @default(0)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  tenant       Tenant       @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  warehouseJob WarehouseJob @relation(fields: [warehouseJobId], references: [id], onDelete: Cascade)

  @@index([tenantId, warehouseJobId])
  @@index([warehouseJobId, sortOrder])
  @@map("warehouse_job_cargo_lines")
}

model WarehouseJobContainer {
  id              String   @id @default(cuid())
  tenantId        String
  warehouseJobId  String
  containerNumber String?
  sealNumber      String?
  notes           String?
  sortOrder       Int      @default(0)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  tenant       Tenant       @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  warehouseJob WarehouseJob @relation(fields: [warehouseJobId], references: [id], onDelete: Cascade)

  @@index([tenantId, warehouseJobId])
  @@index([warehouseJobId, sortOrder])
  @@map("warehouse_job_containers")
}

model transport_order_items {
  id               String                       @id @default(cuid())
  tenantId         String
  transportOrderId String
  inventoryItemId  String
  batchId          String?
  qty              Int
  createdAt        DateTime                     @default(now())
  updatedAt        DateTime                     @updatedAt
  tenant           Tenant                       @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  transport_order  TransportOrder               @relation(fields: [transportOrderId], references: [id], onDelete: Cascade)
  inventory_item   inventory_items              @relation(fields: [inventoryItemId], references: [id], onDelete: Cascade)
  batch            inventory_batches?           @relation(fields: [batchId], references: [id], onDelete: SetNull)
  units            transport_order_item_units[]

  @@unique([transportOrderId, inventoryItemId])
  @@index([tenantId, transportOrderId])
  @@index([tenantId, inventoryItemId])
  @@index([tenantId, batchId])
  @@map("transport_order_items")
}

model transport_order_item_units {
  id                   String   @id @default(cuid())
  tenantId             String
  transportOrderItemId String
  inventoryUnitId      String
  createdAt            DateTime @default(now())

  tenant         Tenant                @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  order_item     transport_order_items @relation(fields: [transportOrderItemId], references: [id], onDelete: Cascade)
  inventory_unit inventory_units       @relation(fields: [inventoryUnitId], references: [id], onDelete: Cascade)

  @@unique([transportOrderItemId, inventoryUnitId])
  @@index([tenantId, transportOrderItemId])
  @@index([tenantId, inventoryUnitId])
  @@map("transport_order_item_units")
}

model Invoice {
  id       String @id @default(cuid())
  tenantId String

  invoiceNo    String
  customerName String
  customerCompanyId String?
  sourceJobId       String?
  /// Optional ACCEPTED CustomerQuotation governing this invoice. Null for manual/legacy invoices.
  sourceCustomerQuotationId String?
  templateCode      String @default("DB_WISDOM")
  currency     String @default("SGD")

  issueDate DateTime  @default(now())
  dueDate   DateTime?

  notes  String?
  /// Canonical lifecycle: DRAFT → GENERATED → ISSUED → PAID, plus VOID.
  /// pdfGeneratedAt is a snapshot timestamp, not a status. Sent is not stored.
  status InvoiceStatus @default(DRAFT)

  subtotalCents Int
  taxCents      Int
  totalCents    Int

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // NEW
  issuedAt       DateTime?
  issuedByUserId String?
  snapshot       Json?
  pdfKey         String?
  pdfGeneratedAt DateTime?
  lockedAt       DateTime?

  // relations
  orders    TransportOrder[]
  lineItems InvoiceLineItem[]
  tenant    Tenant            @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  sentAt         DateTime?
  sentByUserId   String?
  paidAt         DateTime?
  paidByUserId   String?
  chargeReservations InvoiceChargeReservation[]
  sourceCustomerQuotation CustomerQuotation? @relation("InvoiceSourceCustomerQuotation", fields: [sourceCustomerQuotationId], references: [id], onDelete: SetNull)

  @@unique([tenantId, invoiceNo])
  @@index([tenantId, createdAt])
  @@index([tenantId, status])
  @@index([tenantId, sourceJobId])
  @@index([tenantId, sourceJobId, status])
  @@index([tenantId, sourceCustomerQuotationId])
  @@index([tenantId, paidAt])
  @@map("invoices")
}

model InvoiceLineItem {
  id        String @id @default(cuid())
  tenantId  String
  invoiceId String

  description    String
  qty            Int
  unitPriceCents Int
  amountCents    Int

  taxCode  String // "SR" | "ZR"
  taxRate  Int // store as integer percent*100? We'll store 900 = 9.00%, 0 = 0
  taxCents Int
  sourceType String? // JOB | QUOTATION_MASTER | MANUAL
  /// Provenance to the JobCharge this line was snapshotted from. Null for manual/legacy lines.
  jobChargeId String?
  sourceMasterItemId String?
  sourceTripId String?
  tripDisplayRefSnapshot String?
  requiresManualAmount Boolean @default(false)

  createdAt DateTime @default(now())

  invoice Invoice @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  tenant  Tenant  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  jobCharge JobCharge? @relation("InvoiceLineItemJobCharge", fields: [jobChargeId], references: [id], onDelete: SetNull)

  @@index([tenantId, invoiceId])
  @@index([tenantId, jobChargeId])
  @@map("invoice_line_items")
}

/// Active JobCharge reservation: one reserving invoice per charge. Released on Void or Draft line removal.
model InvoiceChargeReservation {
  id          String   @id @default(cuid())
  tenantId    String
  invoiceId   String
  jobChargeId String
  createdAt   DateTime @default(now())

  tenant    Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  invoice   Invoice   @relation(fields: [invoiceId], references: [id], onDelete: Cascade)
  jobCharge JobCharge @relation(fields: [jobChargeId], references: [id], onDelete: Cascade)

  @@unique([tenantId, jobChargeId])
  @@index([tenantId, invoiceId])
  @@map("invoice_charge_reservations")
}

model transport_internal_ref_counters {
  tenantId String
  yyyymm   String
  nextSeq  Int

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@id([tenantId, yyyymm])
  @@index([tenantId])
  @@map("transport_internal_ref_counters")
}

enum NotificationAudience {
  USER
  ROLE
  TENANT
}

enum NotificationSeverity {
  INFO
  SUCCESS
  WARNING
  DANGER
}

model Notification {
  id           String               @id @default(cuid())
  tenantId     String
  userId       String?
  role         Role?
  audience     NotificationAudience
  type         String
  title        String
  description  String?
  severity     NotificationSeverity @default(INFO)
  entityType   String?
  entityId     String?
  jobId        String?
  tripId       String?
  driverUserId String?
  metadata     Json?
  createdAt    DateTime             @default(now())

  tenant     Tenant                   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  recipients NotificationRecipient[]

  @@index([tenantId, createdAt])
  @@index([tenantId, userId, createdAt])
  @@index([tenantId, role, createdAt])
  @@index([tenantId, type, entityId])
  @@map("notifications")
}

model NotificationRecipient {
  id             String    @id @default(cuid())
  notificationId String
  tenantId       String
  userId         String
  readAt         DateTime?
  createdAt      DateTime  @default(now())

  notification Notification @relation(fields: [notificationId], references: [id], onDelete: Cascade)

  @@unique([notificationId, userId])
  @@index([tenantId, userId, readAt, createdAt])
  @@index([tenantId, userId, createdAt])
  @@map("notification_recipients")
}

enum PushPlatform {
  ios
  android
  unknown
}

model PushDevice {
  id            String       @id @default(cuid())
  tenantId      String
  userId        String
  platform      PushPlatform @default(unknown)
  expoPushToken String       @unique
  deviceId      String?
  appVersion    String?
  lastSeenAt    DateTime     @default(now())
  disabledAt    DateTime?
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId, userId])
  @@index([disabledAt])
  @@map("push_devices")
}

model Chassis {
  id        String   @id @default(cuid())
  tenantId  String
  chassisNo String
  label     String?
  status    String   @default("ACTIVE")
  notes     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant       Tenant        @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  gpsDevices   GpsDevice[]
  gpsPositions GpsPosition[]

  @@unique([tenantId, chassisNo])
  @@index([tenantId])
  @@index([tenantId, status])
  @@map("chassis")
}

model GpsDevice {
  id           String    @id @default(cuid())
  tenantId     String
  chassisId    String?
  vehicleId    String?
  driverId     String?
  terminalId   String    @unique
  imei         String?
  simNumber    String?
  model        String    @default("TK905B-4G")
  protocol     String    @default("JT808")
  isActive     Boolean   @default(true)
  lastSeenAt   DateTime?
  lastLat      Decimal?  @db.Decimal(10, 7)
  lastLng      Decimal?  @db.Decimal(10, 7)
  lastSpeedKph Float?
  lastHeading  Int?
  lastBatteryVoltageMv Int?
  lastBatteryVoltage   Decimal?  @db.Decimal(6, 3)
  lastBatteryPercent   Int?
  lastBatterySeenAt    DateTime?
  lastSignalStrength   Int?
  lastSatelliteCount   Int?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  tenant    Tenant        @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  chassis   Chassis?      @relation(fields: [chassisId], references: [id], onDelete: SetNull)
  vehicle   Vehicle?      @relation(fields: [vehicleId], references: [id], onDelete: SetNull)
  driver    drivers?      @relation(fields: [driverId], references: [id], onDelete: SetNull)
  positions GpsPosition[]

  @@index([tenantId])
  @@index([tenantId, chassisId])
  @@index([tenantId, vehicleId])
  @@index([tenantId, driverId])
  @@map("gps_devices")
}

model GpsPosition {
  id            String   @id @default(cuid())
  tenantId      String
  gpsDeviceId   String
  chassisId     String?
  vehicleId     String?
  driverId      String?
  lat           Decimal  @db.Decimal(10, 7)
  lng           Decimal  @db.Decimal(10, 7)
  speedKph      Float?
  heading       Int?
  altitude      Float?
  batteryVoltageMv Int?
  batteryVoltage   Decimal? @db.Decimal(6, 3)
  batteryPercent   Int?
  signalStrength   Int?
  satelliteCount   Int?
  recordedAt    DateTime
  receivedAt    DateTime @default(now())
  rawProtocol   String?
  rawMessageId  String?
  rawPayload    Json?
  createdAt     DateTime @default(now())

  tenant    Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  gpsDevice GpsDevice @relation(fields: [gpsDeviceId], references: [id], onDelete: Cascade)
  chassis   Chassis?  @relation(fields: [chassisId], references: [id], onDelete: SetNull)
  vehicle   Vehicle?  @relation(fields: [vehicleId], references: [id], onDelete: SetNull)
  driver    drivers?  @relation(fields: [driverId], references: [id], onDelete: SetNull)

  @@index([gpsDeviceId, recordedAt])
  @@index([chassisId, recordedAt])
  @@index([vehicleId, recordedAt])
  @@index([tenantId, chassisId, recordedAt])
  @@index([tenantId, recordedAt])
  @@map("gps_positions")
}

/// Customer-scoped commercial rate template (independent copy; source IDs audit-only).
model CustomerRateTemplate {
  id                          String                     @id @default(cuid())
  tenantId                    String
  customerCompanyId           String
  name                        String
  status                      CustomerRateTemplateStatus @default(DRAFT)
  currency                    String                     @default("SGD")
  effectiveFrom               DateTime?
  effectiveTo                 DateTime?
  notes                       String?
  sourceMasterDatasetId       String?
  sourceMasterDatasetVersionNo Int?
  createdAt                   DateTime                   @default(now())
  createdByUserId             String?
  updatedAt                   DateTime                   @updatedAt
  updatedByUserId             String?

  tenant          Tenant                   @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customerCompany customer_companies       @relation(fields: [customerCompanyId], references: [id], onDelete: Cascade)
  createdBy       User?                    @relation("CustomerRateTemplateCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy       User?                    @relation("CustomerRateTemplateUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  rows            CustomerRateTemplateRow[]
  quotations      CustomerQuotation[]

  @@index([tenantId, customerCompanyId])
  @@index([tenantId, customerCompanyId, status])
  @@map("customer_rate_templates")
}

model CustomerRateTemplateRow {
  id                     String   @id @default(cuid())
  templateId             String
  tenantId               String
  code                   String
  label                  String
  section                String?
  description            String?
  category               String?
  unit                   String?
  containerSize          String?
  tripMode               String?
  areaScope              String?
  currency               String   @default("SGD")
  rateCents              Int?
  rawRateText            String?
  requiresManualAmount   Boolean  @default(false)
  hasMultipleRates       Boolean  @default(false)
  rateOptionsJson        Json?
  defaultRateOptionIndex Int?
  notes                  String?
  sortOrder              Int?
  isActive               Boolean  @default(true)
  metadataJson           Json?
  sourceMasterRowId      String?
  createdAt              DateTime @default(now())
  createdByUserId        String?
  updatedAt              DateTime @updatedAt
  updatedByUserId        String?

  template CustomerRateTemplate @relation(fields: [templateId], references: [id], onDelete: Cascade)
  tenant   Tenant               @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@index([tenantId, templateId])
  @@index([tenantId, code])
  @@index([tenantId, isActive, sortOrder])
  @@map("customer_rate_template_rows")
}

/// Structured customer quotation (commercial offer). Distinct from CustomerCompanyQuotation PDF archive.
model CustomerQuotation {
  id                           String                            @id @default(cuid())
  tenantId                     String
  customerCompanyId            String
  quotationNo                  String
  title                        String?
  status                       CustomerQuotationStatus           @default(DRAFT)
  currency                     String                            @default("SGD")
  issueDate                    DateTime?
  validFrom                    DateTime?
  validUntil                   DateTime?
  notes                        String?
  sourceTemplateId             String?
  sourceTemplateNameSnapshot   String?
  subtotalCents                Int                               @default(0)
  taxCents                     Int                               @default(0)
  totalCents                   Int                               @default(0)
  issuedAt                     DateTime?
  issuedByUserId               String?
  lockedAt                     DateTime?
  acceptedAt                   DateTime?
  acceptedByUserId             String?
  acceptanceMethod             CustomerQuotationAcceptanceMethod?
  acceptanceEvidenceNote       String?
  acceptanceEvidenceStorageKey String?
  rejectedAt                   DateTime?
  voidedAt                     DateTime?
  expiredAt                    DateTime?
  customerNameSnapshot        String?
  /// Generated (unsigned) quotation PDF — never overwritten by signed upload.
  pdfKey                       String?
  pdfGeneratedAt               DateTime?
  /// Signed customer copy (separate from generated PDF).
  signedDocumentKey            String?
  signedDocumentOriginalName   String?
  signedDocumentUploadedAt     DateTime?
  signedDocumentUploadedByUserId String?
  signedDocumentVersion        Int                               @default(0)
  createdAt                    DateTime                          @default(now())
  createdByUserId              String?
  updatedAt                    DateTime                          @updatedAt
  updatedByUserId              String?

  tenant          Tenant                 @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  customerCompany customer_companies     @relation(fields: [customerCompanyId], references: [id], onDelete: Restrict)
  sourceTemplate  CustomerRateTemplate?  @relation(fields: [sourceTemplateId], references: [id], onDelete: SetNull)
  createdBy       User?                  @relation("CustomerQuotationCreatedBy", fields: [createdByUserId], references: [id], onDelete: SetNull)
  updatedBy       User?                  @relation("CustomerQuotationUpdatedBy", fields: [updatedByUserId], references: [id], onDelete: SetNull)
  issuedBy        User?                  @relation("CustomerQuotationIssuedBy", fields: [issuedByUserId], references: [id], onDelete: SetNull)
  acceptedBy      User?                  @relation("CustomerQuotationAcceptedBy", fields: [acceptedByUserId], references: [id], onDelete: SetNull)
  lines           CustomerQuotationLine[]
  jobs            Job[]                  @relation("JobSourceCustomerQuotation")
  invoices        Invoice[]              @relation("InvoiceSourceCustomerQuotation")

  @@unique([tenantId, quotationNo])
  @@index([tenantId, customerCompanyId])
  @@index([tenantId, customerCompanyId, status])
  @@index([tenantId, status, validUntil])
  @@map("customer_quotations")
}

model CustomerQuotationLine {
  id                   String   @id @default(cuid())
  quotationId          String
  tenantId             String
  sortOrder            Int      @default(0)
  code                 String
  label                String
  description          String?
  unit                 String?
  qty                  Float    @default(1)
  unitPriceCents       Int      @default(0)
  amountCents          Int      @default(0)
  currency             String   @default("SGD")
  taxCode              String   @default("SR")
  taxRate              Int      @default(900)
  taxCents             Int      @default(0)
  requiresManualAmount Boolean  @default(false)
  sourceTemplateRowId  String?
  sourceMasterRowId    String?
  metadataJson         Json?
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  quotation CustomerQuotation @relation(fields: [quotationId], references: [id], onDelete: Cascade)
  tenant    Tenant            @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  jobCharges JobCharge[]      @relation("JobChargeSourceCustomerQuotationLine")

  @@index([tenantId, quotationId])
  @@index([tenantId, code])
  @@map("customer_quotation_lines")
}

model quotation_no_counters {
  tenantId  String
  yyyymm    String
  nextSeq   Int
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@id([tenantId, yyyymm])
  @@index([tenantId])
  @@map("quotation_no_counters")
}

enum CustomerRateTemplateStatus {
  DRAFT
  ACTIVE
  ARCHIVED
}

enum CustomerCommercialStatus {
  PROSPECT
  PENDING_COMMERCIAL_APPROVAL
  ACTIVE
  SUSPENDED
}

enum CustomerQuotationStatus {
  DRAFT
  ISSUED
  SIGNED
  ACCEPTED
  REJECTED
  EXPIRED
  VOID
  CANCELLED
}

enum CustomerQuotationAcceptanceMethod {
  EMAIL
  PORTAL
  PHONE
  SIGNED_DOCUMENT
  OTHER
}

/// Stores idempotent create outcomes for safe client retries.
enum IdempotencyRecordStatus {
  PENDING
  COMPLETED
}

model IdempotencyRecord {
  id           String                  @id @default(cuid())
  tenantId     String
  scope        String                  @db.VarChar(64)
  operationKey String                  @db.VarChar(128)
  requestHash  String                  @db.VarChar(64)
  status       IdempotencyRecordStatus @default(PENDING)
  resourceType String?                 @db.VarChar(64)
  resourceId   String?                 @db.VarChar(128)
  claimedAt    DateTime                @default(now())
  completedAt  DateTime?
  createdAt    DateTime                @default(now())

  tenant Tenant @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, scope, operationKey])
  @@index([tenantId, resourceType, resourceId])
  @@index([tenantId, status, claimedAt])
  @@map("idempotency_records")
}

/// Driver-submitted trip expense (canonical amount/currency). Receipts are evidence only.
enum TripExpenseCategory {
  PARKING
  TOLL
  FUEL
  PORT_FEE
  ERP_CHARGE
  CASH_CARD
  MEAL
  MISCELLANEOUS
  OTHER
}

enum TripExpensePaymentMethod {
  COMPANY_EPAYMENT
  DRIVER_PAID
  COMPANY_CASH
  OTHER
}

enum TripExpenseReviewStatus {
  PENDING_REVIEW
  APPROVED
  REJECTED
  NEEDS_CLARIFICATION
}

enum TripExpenseReimbursementStatus {
  NOT_REQUIRED
  PENDING
  PAID
}

enum TripExpenseEventAction {
  SUBMITTED
  ATTACHMENT_ADDED
  UPDATED
  RESUBMITTED
  CLARIFICATION_REQUESTED
  APPROVED
  REJECTED
  REVIEWER_CORRECTED
  REIMBURSEMENT_MARKED_PAID
  REIMBURSEMENT_REOPENED
}

model TripExpense {
  id                      String                         @id @default(cuid())
  tenantId                String
  jobId                   String
  tripId                  String
  submittedByUserId       String
  /// Optional drivers.id when a driver profile exists for the submitter.
  submittedByDriverId     String?
  category                TripExpenseCategory
  paymentMethod           TripExpensePaymentMethod
  /// Canonical monetary amount in minor units (cents). Receipts are not the SoT.
  amountCents             Int
  currency                String                         @db.VarChar(3)
  transactionDate         DateTime                       @db.Date
  remarks                 String?                        @db.Text
  reviewStatus            TripExpenseReviewStatus        @default(PENDING_REVIEW)
  reviewedByUserId        String?
  reviewedAt              DateTime?
  reviewReason            String?                        @db.Text
  reimbursementStatus     TripExpenseReimbursementStatus @default(NOT_REQUIRED)
  reimbursedAt            DateTime?
  reimbursedByUserId      String?
  createdAt               DateTime                       @default(now())
  updatedAt               DateTime                       @updatedAt

  tenant              Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  job                 Job                     @relation(fields: [jobId], references: [id], onDelete: Cascade)
  trip                Trip                    @relation(fields: [tripId], references: [id], onDelete: Cascade)
  submittedByUser     User                    @relation("TripExpenseSubmittedBy", fields: [submittedByUserId], references: [id], onDelete: Restrict)
  submittedByDriver   drivers?                @relation(fields: [submittedByDriverId], references: [id], onDelete: SetNull)
  reviewedByUser      User?                   @relation("TripExpenseReviewedBy", fields: [reviewedByUserId], references: [id], onDelete: SetNull)
  reimbursedByUser    User?                   @relation("TripExpenseReimbursedBy", fields: [reimbursedByUserId], references: [id], onDelete: SetNull)
  attachments         TripExpenseAttachment[]
  events              TripExpenseEvent[]

  @@index([tenantId, reviewStatus, createdAt])
  @@index([tenantId, reimbursementStatus, createdAt])
  @@index([tenantId, tripId, createdAt])
  @@index([tenantId, jobId, createdAt])
  @@index([tenantId, jobId, reviewStatus])
  @@index([tenantId, submittedByUserId, createdAt])
  @@index([tenantId, transactionDate])
  @@map("trip_expenses")
}

model TripExpenseAttachment {
  id               String   @id @default(cuid())
  tenantId         String
  expenseId        String
  storageKey       String
  originalName     String
  mimeType         String
  sizeBytes        Int?
  uploadedByUserId String
  isActive         Boolean  @default(true)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  tenant     Tenant     @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  expense    TripExpense @relation(fields: [expenseId], references: [id], onDelete: Cascade)
  uploadedBy User       @relation("TripExpenseAttachmentUploadedBy", fields: [uploadedByUserId], references: [id], onDelete: Restrict)

  @@index([tenantId, expenseId, isActive])
  @@unique([tenantId, storageKey])
  @@map("trip_expense_attachments")
}

model TripExpenseEvent {
  id               String                 @id @default(cuid())
  tenantId         String
  expenseId        String
  actorUserId      String?
  action           TripExpenseEventAction
  previousStatus   TripExpenseReviewStatus?
  newStatus        TripExpenseReviewStatus?
  reason           String?                @db.Text
  /// Safe field-level before/after snapshot (no file bytes, signed URLs, or secrets).
  changedFieldsJson Json?
  createdAt        DateTime               @default(now())

  tenant  Tenant     @relation(fields: [tenantId], references: [id], onDelete: Cascade)
  expense TripExpense @relation(fields: [expenseId], references: [id], onDelete: Cascade)
  actor   User?      @relation("TripExpenseEventActor", fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([tenantId, expenseId, createdAt])
  @@map("trip_expense_events")
}
