node_modules
*.zip
dist
tsconfig.tsbuildinfo
tsconfig.build.tsbuildinfo

# Local environment files. Keep tracked templates such as .env.example.
.env
.env.*
!.env.example

# Run-owned UAT automation manifests (may contain tenant UUIDs)
scripts/.e2e-uat-last-run.json
